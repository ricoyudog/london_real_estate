{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nan-fung.local/schemas/agent_tool_request.v1.schema.json",
  "title": "Agent tool request v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "request_id", "arguments", "host_context"],
  "properties": {
    "schema_version": {"const": "agent_tool_request.v1"},
    "request_id": {"$ref": "#/$defs/non_empty_string"},
    "arguments": {"type": "object"},
    "host_context": {"$ref": "#/$defs/host_context"}
  },
  "$defs": {
    "host_context": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "principal",
        "capability_scope_id",
        "turn_id",
        "tool_call_id",
        "allowed_access_classes",
        "allowed_capability_ids",
        "allowed_refresh_profiles"
      ],
      "properties": {
        "principal": {"$ref": "#/$defs/non_empty_string"},
        "capability_scope_id": {"$ref": "#/$defs/non_empty_string"},
        "turn_id": {"$ref": "#/$defs/non_empty_string"},
        "tool_call_id": {"$ref": "#/$defs/non_empty_string"},
        "refresh_request_id": {"$ref": "#/$defs/non_empty_string"},
        "allowed_access_classes": {"$ref": "#/$defs/non_empty_strings"},
        "allowed_capability_ids": {"$ref": "#/$defs/strings"},
        "allowed_refresh_profiles": {"$ref": "#/$defs/strings"}
      }
    },
    "strings": {
      "type": "array",
      "maxItems": 64,
      "items": {"$ref": "#/$defs/non_empty_string"},
      "uniqueItems": true
    },
    "non_empty_string": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "pattern": "\\S"
    },
    "non_empty_strings": {
      "allOf": [
        {"$ref": "#/$defs/strings"},
        {"minItems": 1}
      ]
    }
  }
}
